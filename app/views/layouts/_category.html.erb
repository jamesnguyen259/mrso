<aside class="single_sidebar_widget post_category_widget">
    <h4 class="widget_title">Film Catgories</h4>
    <ul class="list cat-list">
      <% @categories.each do |c| %>
      <li>
        <%= link_to c.name, c, class: "d-flex justify-content-between" %>
      </li>
      <% end %>
    </ul>
    <div class="br"></div>
</aside>
